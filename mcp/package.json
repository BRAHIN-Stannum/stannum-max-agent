{
  "name": "stannum-max-mcp",
  "version": "1.0.0",
  "description": "MCP server para MAX — Agente DITEG STANNUM. Lee y escribe en el repo stannum-max-agent vía GitHub API.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
